{
  "group": {
    "default": {
      "targets": ["redbot"]
    }
  },
  "target": {
    "redbot": {
      "context": "red",
      "dockerfile": "Dockerfile",
      "tags": ["ghcr.io/cssnr/carlbot-red:latest"],
      "platforms": ["linux/amd64", "linux/arm64"],
      "output": ["type=image,push=true"]
    }
  }
}
